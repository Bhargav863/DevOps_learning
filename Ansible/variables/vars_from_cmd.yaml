- name: variables from the command line
  hosts: localhost
  tasks:
    - name: Read the variable from command line
      ansible.builtin.debug:
        msg: I am learning {{ course }}
# command to run playbook.
# ansible-playbook -e course=ansible vars_from_cmd.yaml
